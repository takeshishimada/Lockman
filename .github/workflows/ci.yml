name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - '*'
  workflow_dispatch:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  xcodebuild-latest:
    name: xcodebuild (16)
    runs-on: macos-15
    strategy:
      matrix:
        command: [test, '']
        platform: [IOS, MACOS]
        xcode: ['16.2']
    steps:
      - uses: actions/checkout@v4
      - name: Select Xcode ${{ matrix.xcode }}
        run: |
          # Apply workaround for macOS 15 iOS build issues
          # Reference: https://github.com/actions/runner-images/issues/12758#issuecomment-3187115656
          sudo xcode-select -s /Applications/Xcode_${{ matrix.xcode }}.app
          # Additional workaround: explicitly switch to Xcode app
          sudo xcode-select --switch /Applications/Xcode_${{ matrix.xcode }}.app
          # Verify Xcode selection
          xcode-select -p
      - name: Update xcbeautify
        run: brew update && brew upgrade xcbeautify
      - name: Install iOS platform and setup
        run: |
          sudo xcodebuild -runFirstLaunch
          # Install iOS platform to ensure SDK availability
          sudo xcodebuild -downloadPlatform iOS || true
          # Try specific iOS version downloads
          sudo xcodebuild -downloadPlatform iOS -buildVersion 18.2 || true
          sudo xcodebuild -downloadPlatform iOS -buildVersion 18.1 || true
          # Accept Xcode license
          sudo xcodebuild -license accept || true
          # Reset simulator device list
          sudo xcrun simctl shutdown all
          sudo xcrun simctl erase all
          # List available platforms for debugging
          xcodebuild -showsdks
          # List available simulators
          xcrun simctl list devices available
      - name: List available devices
        run: xcrun simctl list devices available
      - name: Cache derived data
        uses: actions/cache@v3
        with:
          path: |
            ~/.derivedData
          key: |
            deriveddata-xcodebuild-${{ matrix.platform }}-${{ matrix.xcode }}-${{ matrix.command }}-${{ hashFiles('**/Sources/**/*.swift', '**/Tests/**/*.swift') }}
          restore-keys: |
            deriveddata-xcodebuild-${{ matrix.platform }}-${{ matrix.xcode }}-${{ matrix.command }}-
      - name: Set IgnoreFileSystemDeviceInodeChanges flag
        run: defaults write com.apple.dt.XCBuild IgnoreFileSystemDeviceInodeChanges -bool YES
      - name: Update mtime for incremental builds
        uses: chetan/git-restore-mtime-action@v2
      - name: Debug
        run: make XCODEBUILD_ARGUMENT="${{ matrix.command }}" CONFIG=Debug PLATFORM="${{ matrix.platform }}" WORKSPACE=.github/package.xcworkspace xcodebuild

  examples:
    name: Examples
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4
      - name: Cache derived data
        uses: actions/cache@v3
        with:
          path: ~/.derivedData
          key: |
            deriveddata-examples-${{ hashFiles('**/Sources/**/*.swift', '**/Tests/**/*.swift', '**/Examples/**/*.swift') }}
          restore-keys: |
            deriveddata-examples-
      - name: Select Xcode 16
        run: |
          # Apply workaround for macOS 15 iOS build issues
          # Reference: https://github.com/actions/runner-images/issues/12758#issuecomment-3187115656
          sudo xcode-select -s /Applications/Xcode_16.2.app
          # Additional workaround: explicitly switch to Xcode app
          sudo xcode-select --switch /Applications/Xcode_16.2.app
          # Verify Xcode selection
          xcode-select -p
      - name: Update xcbeautify
        run: brew update && brew upgrade xcbeautify
      - name: Install iOS platform and setup
        run: |
          sudo xcodebuild -runFirstLaunch
          # Install iOS platform to ensure SDK availability
          sudo xcodebuild -downloadPlatform iOS || true
          # Try specific iOS version downloads
          sudo xcodebuild -downloadPlatform iOS -buildVersion 18.2 || true
          sudo xcodebuild -downloadPlatform iOS -buildVersion 18.1 || true
          # Accept Xcode license
          sudo xcodebuild -license accept || true
          # Reset simulator device list
          sudo xcrun simctl shutdown all
          sudo xcrun simctl erase all
          # List available platforms for debugging
          xcodebuild -showsdks
          # List available simulators
          xcrun simctl list devices available
      - name: Set IgnoreFileSystemDeviceInodeChanges flag
        run: defaults write com.apple.dt.XCBuild IgnoreFileSystemDeviceInodeChanges -bool YES
      - name: Update mtime for incremental builds
        uses: chetan/git-restore-mtime-action@v2
      - name: Strategies
        run: |
          cd Examples/Strategies
          xcodebuild build \
            -project Strategies.xcodeproj \
            -scheme "Strategies" \
            -destination "platform=iOS Simulator,name=iPhone 16 Pro" \
            -derivedDataPath ~/.derivedData \
            -skipMacroValidation \
            -skipPackagePluginValidation
